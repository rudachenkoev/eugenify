import { Values as ColorValues } from './colors'
import { Values as SizeValues } from './sizes'
import { Values as VariantValues } from './variants'

const IconTypeValues = ['filled', 'outlined', 'round', 'sharp', 'two-tone']

export { ColorValues, IconTypeValues, SizeValues, VariantValues }
